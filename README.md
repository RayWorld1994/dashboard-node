# dashboard-node

Super basic Node.js app.

## Run

```bash
npm start
```

Then open `http://localhost:3000` (or `3001` if `3000` is already in use).

## Configure

- `PORT`: change the listening port (default `3000`)

